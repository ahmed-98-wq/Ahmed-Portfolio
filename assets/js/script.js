  // ---- translations ----
  const translations = {
    en: {
      'a11y.skip': 'Skip to main content',
      'a11y.portraitAlt': 'Portrait of Ahmed Adam',
      'a11y.project1Alt': 'E-commerce platform project interface',
      'a11y.project2Alt': 'Zakii company website project interface',
      'a11y.project3Alt': 'Skyway travel agency project interface',
      'a11y.downloadCv': 'Download CV',
      'nav.name': 'Ahmed Adam',
      'nav.about': 'About',
      'nav.services': 'Services',
      'nav.skills': 'Skills',
      'nav.projects': 'Projects',
      'nav.proof': 'Proof',
      'nav.experience': 'Experience',
      'nav.contact': 'Contact',
      'nav.cta': "Let's start",
      'hero.eyebrow': 'Available for new projects and remote collaboration.',
      'hero.title': 'I design and build <span>fast, clear websites</span> for small businesses and service teams.',
      'hero.lead': "I'm Ahmed Adam. I turn content and services into a clear, responsive web experience—from structuring the idea and designing the interface to code and launch.",
      'hero.noteLabel': 'Notes from the desk',
      'hero.noteQuote': '“Good design should not need to explain itself too much.”',
      'hero.note1': 'Understand the problem before opening the code editor.',
      'hero.note2': 'Design for mobile first, then expand the experience.',
      'hero.note3': 'Review the small details before delivery.',
      'hero.ctaProjects': 'See my work',
      'hero.ctaContact': 'Get in touch',
      'hero.ctaCv': 'Download CV',
      'hero.stat1': 'years of experience',
      'hero.stat2': 'live projects',
      'hero.stat3': 'bilingual communication',
      'about.tag': '01 — About',
      'about.title': 'A software engineer who loves the small details',
      'about.subtitle': 'A solid technical background, an eye for interface detail, and care for product performance from day one.',
      'about.row1k': 'Name',
      'about.row1v': 'Ahmed Adam',
      'about.row2k': 'Role',
      'about.row2v': 'Web App Designer & Frontend Developer',
      'about.row3k': 'Location',
      'about.row3v': 'Khartoum, Sudan',
      'about.row4k': 'Remote work',
      'about.row4v': 'Available',
      'about.row5k': 'Languages',
      'about.row5v': 'Arabic / English',
      'about.p1': 'My programming journey began with a simple question: how does a blank screen transform into an application used by thousands of people? That curiosity evolved into a career, and I have since worked on a variety of projects.',
      'about.p2': 'My focus is on <strong>performance and clarity</strong>: clean, readable code, and interfaces that feel meticulously built, not just "functional." I prioritize <strong>user experience</strong> and <strong>accessibility</strong>, and have experience in database design.',
      'about.p3': 'Outside of work, I keep up with modern web development trends and contribute to open-source projects whenever I have the time.',
      'services.tag': '02 — How I can help',
      'services.title': 'Services that start with the problem, not a template',
      'services.subtitle': 'I help individuals and small teams turn an idea into a clear, fast website that works well across devices.',
      'services.s1.title': 'Web interface design',
      'services.s1.desc': 'Structuring content and designing calm interfaces that reflect the project identity and help users find what they need.',
      'services.s1.meta': 'Content structure · UI · Responsive',
      'services.s2.title': 'Frontend development',
      'services.s2.desc': 'Turning designs into fast, responsive pages with organized code and close attention to detail and accessibility.',
      'services.s2.meta': 'HTML · CSS · JavaScript',
      'services.s3.title': 'Improving an existing site',
      'services.s3.desc': 'Reviewing the current experience and fixing mobile layout, performance, and unclear navigation issues.',
      'services.s3.meta': 'UX Review · Performance · Fixes',
      'services.s4.title': 'Ongoing support',
      'services.s4.desc': 'Adding new pages and features, resolving issues, and maintaining quality after the site launches.',
      'services.s4.meta': 'Updates · Maintenance · Support',
      'skills.tag': '03 — Tools',
      'skills.title': 'Skills & Technologies',
      'skills.subtitle': 'The toolkit I use daily to build complete products, front to back.',
      'skills.g1': 'Frontend',
      'skills.g2': 'Backend & Architecture',
      'skills.g3': 'Tools & Deployment',
      'projects.tag': '04 — My Work',
      'projects.title': 'Selected Projects',
      'projects.subtitle': 'A selection of websites I designed and developed, with a focus on responsiveness and a clear user experience.',
      'projects.p1.title': 'E-commerce Platform',
      'projects.p1.desc': 'A responsive e-commerce website that presents products in a clear, accessible shopping experience.',
      'projects.case.role': 'Role',
      'projects.case.challenge': 'Challenge',
      'projects.case.solution': 'Solution',
      'projects.case.outcome': 'Outcome',
      'projects.p1.role': 'Interface design and development',
      'projects.p1.challenge': 'Organizing products while keeping navigation clear on smaller screens.',
      'projects.p1.solution': 'A responsive product grid and clear cards that reduce the steps between discovery and product details.',
      'projects.p1.outcome': 'A responsive interface with a direct path from browsing to product details.',
      'projects.p2.title': 'Zakii-multi-production-LTD',
      'projects.p2.desc': "A company website that presents its products, prices, and product gallery in a clear layout.",
      'projects.p2.role': 'Design and frontend development',
      'projects.p2.challenge': 'Presenting products, prices, and imagery without overcrowding the page.',
      'projects.p2.solution': 'Short visual sections create a clear sequence for products, prices, and the image gallery.',
      'projects.p2.outcome': 'A lightweight site that makes the company products easy to browse on mobile and desktop.',
      'projects.p3.title': 'Skyway Agency website',
      'projects.p3.desc': 'A travel agency interface that helps customers explore trips and hotel booking services.',
      'projects.p3.role': 'Interface design and development',
      'projects.p3.challenge': 'Bringing destinations, travel services, and hotels together in one simple experience.',
      'projects.p3.solution': 'A journey organized from destination discovery and services to a clear booking request.',
      'projects.p3.outcome': 'A clear path that guides visitors from exploring a destination to requesting a booking.',
      'projects.live': 'Live demo ↗',
      'projects.code': 'Source code ↗',
      'proof.tag': '05 — Work evidence',
      'proof.title': 'Proof you can try, not promises alone',
      'proof.subtitle': 'No anonymous testimonials or marketing numbers—only trust signals connected to published work and public accounts.',
      'proof.p1.title': 'Working websites',
      'proof.p1.desc': 'Three live experiences you can open and test on mobile and desktop.',
      'proof.p1.link': 'Explore projects ↓',
      'proof.p2.title': 'Public repositories',
      'proof.p2.desc': 'Code and work history are available for review on GitHub.',
      'proof.p2.link': 'Open GitHub ↗',
      'proof.p3.title': 'Clear communication',
      'proof.p3.desc': 'Communicate in Arabic or English, with remote collaboration available.',
      'proof.p3.link': 'Start a conversation ↓',
      'exp.tag': '06 — Career Path',
      'exp.title': 'Experience & Education',
      'exp.subtitle': 'Key milestones in chronological order across my professional and academic journey.',
      'exp.t1.meta': '2026 — Present',
      'exp.t1.title': 'Technical support — Alsawaid Alkhadraa Organization',
      'exp.t1.desc': 'Contributing technical support, monitoring, and evaluation as part of my work.',
      'exp.t2.meta': '2025',
      'exp.t2.title': 'Website Designer and Developer — Travel Agency',
      'exp.t2.desc': 'Designed and developed a responsive travel agency website, focusing on clear services and mobile usability.',
      'exp.t3.meta': '2022',
      'exp.t3.title': "Bachelor of Science (Honours) in Information Technology",
      'exp.t3.desc': 'University of Science and Technology — built a graduation project that presents student results in an organized and accessible way.',
      'contact.title': "Have a project? Let's define the next step",
      'contact.subtitle': 'Send the website type, key pages or features, and expected timeline. I will review the details and return with a clear next step.',
      'contact.whatsapp': 'Message me on WhatsApp',
      'contact.location': 'Sudan · Available remotely',
      'contact.term1': '> preparing message...',
      'contact.term2': '> click the button below to copy ↓',
      'contact.copy': 'Copy email address',
      'contact.noteLabel': 'What should your message include?',
      'contact.noteBody': 'Project type · primary goal · required pages or features · expected timeline. No long brief is needed.',
      'footer.rights': '© 2026 Ahmed Adam. All rights reserved.',
    }
  };

  const I18N_KEY = 'portfolio-lang';
  const THEME_KEY = 'portfolio-theme';
  const pageMeta = {
    ar: {
      title: 'أحمد آدم — مصمم ومطوّر واجهات ويب',
      description: 'أصمّم وأطوّر مواقع سريعة وواضحة للشركات الصغيرة وفرق الخدمات، من الفكرة إلى الإطلاق.',
      locale: 'ar_SD'
    },
    en: {
      title: 'Ahmed Adam — Web Designer & Frontend Developer',
      description: 'Fast, clear and responsive websites for small businesses and service teams, from idea to launch.',
      locale: 'en_US'
    }
  };
  const originalContent = new Map();
  const originalAttributes = new Map();

  document.querySelectorAll('[data-i18n], [data-i18n-html]').forEach(el => {
    const key = el.dataset.i18n || el.dataset.i18nHtml;
    const isHtml = !!el.dataset.i18nHtml;
    originalContent.set(el, { key, isHtml, value: isHtml ? el.innerHTML : el.textContent });
  });
  document.querySelectorAll('[data-i18n-alt], [data-i18n-aria]').forEach(el => {
    const attr = el.dataset.i18nAlt ? 'alt' : 'aria-label';
    const key = el.dataset.i18nAlt || el.dataset.i18nAria;
    originalAttributes.set(el, { attr, key, value: el.getAttribute(attr) });
  });

  function applyLanguage(lang){
    document.documentElement.lang = lang;
    document.documentElement.dir = lang === 'ar' ? 'rtl' : 'ltr';
    document.getElementById('langLabel').textContent = lang === 'ar' ? 'EN' : 'AR';
    document.getElementById('langBtn').setAttribute('lang', lang === 'ar' ? 'en' : 'ar');
    document.getElementById('langBtn').setAttribute('aria-label', lang === 'ar' ? 'Switch to English' : 'التبديل إلى العربية');
    document.querySelector('nav').setAttribute('aria-label', lang === 'ar' ? 'التنقل الرئيسي' : 'Main navigation');
    document.querySelector('.logo').setAttribute('aria-label', lang === 'ar' ? 'العودة إلى بداية الصفحة' : 'Back to the top');
    document.getElementById('menuBtn').setAttribute('aria-label', lang === 'ar' ? 'فتح القائمة' : 'Open menu');
    document.getElementById('themeBtn').setAttribute('aria-label', lang === 'ar' ? 'تبديل الوضع الداكن/الفاتح' : 'Toggle dark/light mode');
    const meta = pageMeta[lang];
    document.title = meta.title;
    document.querySelector('meta[name="description"]').content = meta.description;
    document.querySelector('meta[property="og:title"]').content = meta.title;
    document.querySelector('meta[property="og:description"]').content = meta.description;
    document.querySelector('meta[property="og:locale"]').content = meta.locale;
    document.querySelector('meta[name="twitter:title"]').content = meta.title;
    document.querySelector('meta[name="twitter:description"]').content = meta.description;

    originalContent.forEach((data, el) => {
      const dict = translations.en[data.key];
      const value = (lang === 'en' && dict) ? dict : data.value;
      if(data.isHtml){ el.innerHTML = value; } else { el.textContent = value; }
    });
    originalAttributes.forEach((data, el) => {
      const translated = translations.en[data.key];
      el.setAttribute(data.attr, lang === 'en' && translated ? translated : data.value);
    });
    const liveStatus = document.getElementById('copyStatus');
    if(liveStatus){ liveStatus.textContent = ''; }

    localStorage.setItem(I18N_KEY, lang);
    if(typeof typeSequence === 'function'){ typeSequence(lang); }
  }

  const langBtn = document.getElementById('langBtn');
  langBtn.addEventListener('click', () => {
    const current = document.documentElement.lang === 'ar' ? 'ar' : 'en';
    applyLanguage(current === 'ar' ? 'en' : 'ar');
  });

  // ---- theme toggle ----
  function applyTheme(theme){
    document.documentElement.setAttribute('data-theme', theme);
    document.getElementById('themeIcon').textContent = theme === 'dark' ? '☀️' : '🌙';
    document.getElementById('themeBtn').setAttribute('aria-pressed', theme === 'dark' ? 'true' : 'false');
    document.querySelector('meta[name="theme-color"]').content = theme === 'dark' ? '#0b1020' : '#f6e9c9';
    localStorage.setItem(THEME_KEY, theme);
  }
  const themeBtn = document.getElementById('themeBtn');
  themeBtn.addEventListener('click', () => {
    const current = document.documentElement.getAttribute('data-theme') === 'light' ? 'light' : 'dark';
    applyTheme(current === 'dark' ? 'light' : 'dark');
  });

  // theme is safe to initialize now (no dependency on later DOM/JS)
  const savedTheme = localStorage.getItem(THEME_KEY);
  const prefersLight = window.matchMedia('(prefers-color-scheme: light)').matches;
  applyTheme(savedTheme || (prefersLight ? 'light' : 'dark'));

  // Motion is progressively enhanced so the content remains usable without JavaScript.
  const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  document.body.classList.add('motion-ready');

  // ---- header state + reading progress ----
  const header = document.getElementById('siteHeader');
  const scrollProgress = document.getElementById('scrollProgress');
  let scrollFrame = 0;
  function updateScrollUI(){
    header.classList.toggle('scrolled', window.scrollY > 12);
    const scrollable = document.documentElement.scrollHeight - window.innerHeight;
    const progress = scrollable > 0 ? Math.min(window.scrollY / scrollable, 1) : 0;
    scrollProgress.style.transform = `scaleX(${progress})`;
    scrollFrame = 0;
  }
  window.addEventListener('scroll', () => {
    if(!scrollFrame) scrollFrame = requestAnimationFrame(updateScrollUI);
  }, { passive: true });
  updateScrollUI();

  // ---- mobile menu ----
  const menuBtn = document.getElementById('menuBtn');
  const navLinks = document.getElementById('navLinks');
  menuBtn.addEventListener('click', () => {
    const open = navLinks.classList.toggle('open');
    menuBtn.setAttribute('aria-expanded', open);
  });
  function closeMenu(){
    navLinks.classList.remove('open');
    menuBtn.setAttribute('aria-expanded', 'false');
  }
  document.querySelectorAll('.nav-link').forEach(a => {
    a.addEventListener('click', () => {
      if(window.innerWidth <= 820){ closeMenu(); }
    });
  });
  document.addEventListener('keydown', event => {
    if(event.key === 'Escape' && navLinks.classList.contains('open')){
      closeMenu();
      menuBtn.focus();
    }
  });
  window.addEventListener('resize', () => {
    if(window.innerWidth > 820){ closeMenu(); }
  });

  // ---- active link on scroll ----
  const sections = document.querySelectorAll('main section[id]');
  const links = document.querySelectorAll('.nav-link');
  const navObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if(entry.isIntersecting){
        links.forEach(l => {
          const active = l.getAttribute('href') === '#' + entry.target.id;
          l.classList.toggle('active', active);
          if(active){ l.setAttribute('aria-current', 'page'); }
          else { l.removeAttribute('aria-current'); }
        });
      }
    });
  }, { rootMargin: '-45% 0px -45% 0px' });
  sections.forEach(s => navObserver.observe(s));

  // ---- scroll reveal ----
  const reveals = document.querySelectorAll('.reveal');
  document.querySelectorAll('.services-grid, .skill-groups, .project-grid, .proof-grid, .timeline').forEach(group => {
    [...group.children].filter(el => el.classList.contains('reveal')).forEach((el, index) => {
      el.style.setProperty('--reveal-delay', `${index * 110}ms`);
    });
  });

  if(reduceMotion){
    reveals.forEach(el => el.classList.add('in'));
  } else {
    const revealObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if(entry.isIntersecting){
          entry.target.classList.add('in');
          revealObserver.unobserve(entry.target);
        }
      });
    }, { threshold: 0.12, rootMargin: '0px 0px -8% 0px' });
    reveals.forEach(el => revealObserver.observe(el));
  }

  // ---- restrained pointer movement for the desk note ----
  const heroNote = document.querySelector('.hero-note');
  if(heroNote && !reduceMotion && window.matchMedia('(pointer: fine)').matches){
    let noteFrame = 0;
    heroNote.addEventListener('pointerenter', () => heroNote.classList.add('is-interacting'));
    heroNote.addEventListener('pointermove', event => {
      if(noteFrame) return;
      noteFrame = requestAnimationFrame(() => {
        const rect = heroNote.getBoundingClientRect();
        const x = (event.clientX - rect.left) / rect.width - .5;
        const y = (event.clientY - rect.top) / rect.height - .5;
        heroNote.style.setProperty('--note-rx', `${-y * 3.5}deg`);
        heroNote.style.setProperty('--note-ry', `${x * 4.5}deg`);
        noteFrame = 0;
      });
    });
    heroNote.addEventListener('pointerleave', () => {
      heroNote.classList.remove('is-interacting');
      heroNote.style.setProperty('--note-rx', '0deg');
      heroNote.style.setProperty('--note-ry', '0deg');
    });
  }

  // ---- terminal typing effect ----
  const typeTarget = document.getElementById('typeTarget');
  function getCodeLines(lang){
    const nameStr = lang === 'en' ? '"Ahmed Adam"' : '" أحمد ادم"';
    return [
      { html: '<span class="tk-kw">const</span> <span class="tk-key">developer</span> <span class="tk-punc">=</span> <span class="tk-punc">{</span>' },
      { html: `&nbsp;&nbsp;<span class="tk-key">name</span><span class="tk-punc">:</span> <span class="tk-str">${nameStr}</span><span class="tk-punc">,</span>` },
      { html: '&nbsp;&nbsp;<span class="tk-key">role</span><span class="tk-punc">:</span> <span class="tk-str">"Web Application Designer and Frontend Developer"</span><span class="tk-punc">,</span>' },
      { html: '&nbsp;&nbsp;<span class="tk-key">stack</span><span class="tk-punc">:</span> <span class="tk-punc">[</span><span class="tk-str">"Html,Css,Js"</span><span class="tk-punc">,</span> <span class="tk-str">"PHP"</span><span class="tk-punc">,</span> <span class="tk-str">"Mysql"</span><span class="tk-punc">]</span><span class="tk-punc">,</span>' },
      { html: '&nbsp;&nbsp;<span class="tk-key">available</span><span class="tk-punc">:</span> <span class="tk-kw">true</span><span class="tk-punc">,</span>' },
      { html: '&nbsp;&nbsp;<span class="tk-key">location</span><span class="tk-punc">:</span> <span class="tk-str">"Sudan"</span>' },
      { html: '<span class="tk-punc">}</span><span class="tk-punc">;</span>' },
    ];
  }

  function typeSequence(lang){
    if(!typeTarget) return;
    const codeLines = getCodeLines(lang === 'en' ? 'en' : 'ar');
    typeTarget.innerHTML = '';
    let lineIndex = 0;

    function typeLine(){
      if(lineIndex >= codeLines.length){
        const caret = document.createElement('span');
        caret.className = 'caret';
        typeTarget.appendChild(caret);
        return;
      }
      const lineEl = document.createElement('div');
      lineEl.className = 'term-line';
      typeTarget.appendChild(lineEl);

      const full = codeLines[lineIndex].html;
      const plainLength = full.replace(/<[^>]*>/g, '').replace(/&nbsp;/g, ' ').length;
      let charCount = 0;
      const speed = reduceMotion ? 0 : 14;

      function step(){
        charCount++;
        // reveal proportionally using a temp element trick for HTML-safe partial reveal
        const ratio = Math.min(charCount / plainLength, 1);
        lineEl.innerHTML = full;
        lineEl.style.clipPath = `inset(0 ${100 - ratio * 100}% 0 0)`;
        if(ratio < 1 && !reduceMotion){
          requestAnimationFrame(() => setTimeout(step, speed));
        } else {
          lineEl.style.clipPath = 'none';
          lineIndex++;
          setTimeout(typeLine, 110);
        }
      }
      step();
    }
    typeLine();
  }
  typeSequence(document.documentElement.lang === 'en' ? 'en' : 'ar');

  // ---- copy email ----
  const copyBtn = document.getElementById('copyEmail');
  const copyStatus = document.getElementById('copyStatus');
  copyBtn.addEventListener('click', async () => {
    const lang = document.documentElement.lang === 'en' ? 'en' : 'ar';
    const doneLabel = lang === 'en' ? 'Copied ✓' : 'تم النسخ ✓';
    const defaultLabel = lang === 'en' ? translations.en['contact.copy'] : 'نسخ البريد الإلكتروني';
    try{
      await navigator.clipboard.writeText('ahcinbox1@gmail.com');
      copyBtn.textContent = doneLabel;
      copyStatus.textContent = lang === 'en' ? 'Email address copied.' : 'تم نسخ عنوان البريد الإلكتروني.';
      setTimeout(() => copyBtn.textContent = defaultLabel, 1800);
    }catch(e){
      copyBtn.textContent = 'ahcinbox1@gmail.com';
      copyStatus.textContent = lang === 'en' ? 'Copy failed. The email address is shown on the button.' : 'تعذّر النسخ. يظهر عنوان البريد على الزر.';
    }
  });

  // ---- apply saved language now that all DOM refs (typeTarget, etc.) exist ----
  const savedLang = localStorage.getItem(I18N_KEY);
  if(savedLang && savedLang !== 'ar'){ applyLanguage(savedLang); }
